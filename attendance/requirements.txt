# 런타임
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
jinja2>=3.1
python-multipart>=0.0.9
python-dotenv>=1.0
httpx>=0.27
openpyxl>=3.1
twilio>=9.0
pillow>=10.3

# OCR — Claude Vision 폴백 (선택)
anthropic>=0.40

# OCR — Tesseract (선택). 별도로 tesseract 바이너리 설치 필요:
#   macOS: brew install tesseract / Ubuntu: apt install tesseract-ocr
pytesseract>=0.3.10

# 개발
pytest>=8.0
